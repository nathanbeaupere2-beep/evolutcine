"use client";\n\nimport { useState, useEffect } from "react";\nimport { motion, AnimatePresence } from "framer-motion";\nimport Image from "next/image";\nimport { Play, Info, Volume2, VolumeX } from "lucide-react";\n\ninterface HeroContent {\n  id: string;\n  title: string;\n  description: string;\n  backdropUrl: string;\n  trailerUrl?: string;\n  maturityRating: string;\n  genres: string[];\n  duration?: number;\n}\n\nexport function HeroBanner({ content }: { content: HeroContent }) {\n  const [isMuted, setIsMuted] = useState(true);\n  const [isLoaded, setIsLoaded] = useState(false);\n  const [showVideo, setShowVideo] = useState(false);\n\n  useEffect(() => {\n    const timer = setTimeout(() => {\n      setShowVideo(true);\n    }, 2000);\n    return () => clearTimeout(timer);\n  }, []);\n\n  return (\n    <div className="relative h-[85vh] w-full overflow-hidden">\n      <AnimatePresence mode="wait">\n        {!showVideo ? (\n          <motion.div\n            key="image"\n            initial={{ opacity: 0 }}\n            animate={{ opacity: 1 }}\n            exit={{ opacity: 0 }}\n            transition={{ duration: 1 }}\n            className="absolute inset-0"\n          >\n            <Image\n              src={content.backdropUrl}\n              alt={content.title}\n              fill\n              priority\n              className="object-cover"\n              onLoad={() => setIsLoaded(true)}\n            />\n          </motion.div>\n        ) : (\n          <motion.div\n            key="video"\n            initial={{ opacity: 0 }}\n            animate={{ opacity: 1 }}\n            transition={{ duration: 1 }}\n            className="absolute inset-0"\n          >\n            <video\n              autoPlay\n              muted={isMuted}\n              loop\n              playsInline\n              className="h-full w-full object-cover"\n              src={content.trailerUrl}\n            />\n          </motion.div>\n        )}\n      </AnimatePresence>\n\n      <div className="absolute inset-0 bg-hero-gradient" />\n      <div className="absolute inset-0 bg-gradient-to-r from-netflix-black/80 via-transparent to-transparent" />\n\n      <div className="absolute bottom-0 left-0 right-0 p-8 md:p-16">\n        <motion.div\n          initial={{ opacity: 0, y: 30 }}\n          animate={{ opacity: isLoaded ? 1 : 0, y: isLoaded ? 0 : 30 }}\n          transition={{ duration: 0.8, delay: 0.3 }}\n          className="max-w-2xl space-y-6"\n        >\n          <motion.h1\n            className="text-4xl md:text-6xl lg:text-7xl font-bold text-white drop-shadow-2xl"\n            initial={{ opacity: 0, y: 20 }}\n            animate={{ opacity: 1, y: 0 }}\n            transition={{ delay: 0.5 }}\n          >\n            {content.title}\n          </motion.h1>\n\n          <motion.div\n            className="flex items-center gap-4 text-sm md:text-base text-netflix-lightgray"\n            initial={{ opacity: 0 }}\n            animate={{ opacity: 1 }}\n            transition={{ delay: 0.7 }}\n          >\n            <span className="text-accent-green font-semibold">98% Match</span>\n            <span className="border border-netflix-gray px-1 text-xs">\n              {content.maturityRating}\n            </span>\n            {content.duration && (\n              <span>{Math.floor(content.duration / 60)}h{content.duration % 60}m</span>\n            )}\n          </motion.div>\n\n          <motion.div\n            className="flex flex-wrap gap-2"\n            initial={{ opacity: 0 }}\n            animate={{ opacity: 1 }}\n            transition={{ delay: 0.8 }}\n          >\n            {content.genres.map((genre) => (\n              <span key={genre} className="text-sm text-netflix-lightgray">\n                {genre}\n                <span className="mx-2 text-netflix-gray">•</span>\n              </span>\n            ))}\n          </motion.div>\n\n          <motion.p\n            className="text-lg text-white/90 line-clamp-3 drop-shadow-lg"\n            initial={{ opacity: 0 }}\n            animate={{ opacity: 1 }}\n            transition={{ delay: 0.9 }}\n          >\n            {content.description}\n          </motion.p>\n\n          <motion.div\n            className="flex items-center gap-4 pt-4"\n            initial={{ opacity: 0, y: 20 }}\n            animate={{ opacity: 1, y: 0 }}\n            transition={{ delay: 1 }}\n          >\n            <button className="flex items-center gap-2 bg-white text-black px-8 py-3 rounded-md font-semibold text-lg hover:bg-white/90 transition-all hover:scale-105">\n              <Play className="w-6 h-6 fill-black" />\n              Lecture\n            </button>\n            \n            <button className="flex items-center gap-2 bg-netflix-gray/70 text-white px-8 py-3 rounded-md font-semibold text-lg hover:bg-netflix-gray/90 transition-all hover:scale-105 backdrop-blur-sm">\n              <Info className="w-6 h-6" />\n              Plus d'infos\n            </button>\n\n            {showVideo && (\n              <button\n                onClick={() => setIsMuted(!isMuted)}\n                className="ml-auto p-3 rounded-full border-2 border-white/50 text-white hover:border-white hover:bg-white/10 transition-all"\n              >\n                {isMuted ? <VolumeX className="w-5 h-5" /> : <Volume2 className="w-5 h-5" />}\n              </button>\n            )}\n          </motion.div>\n        </motion.div>\n      </div>\n    </div>\n  );\n}\n